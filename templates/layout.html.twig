<nav class="navbar navbar-expand-lg bg-body-tertiary border-bottom shadow-sm mb-3">
	<div class="container-fluid">
		<a class="navbar-brand fw-bold text-primary" href="{{ path('app_client_index') }}" style="padding:0px">
			<img src="{{ asset('images/ids.png') }}" height="50px" width="50px" class="card-img-top mx-auto" alt="IDS">
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse justify-content-end" id="navbarNav">
			<ul class="navbar-nav align-items-center">
				{% if app.user %}
					{% if app.user and app.user.hasPermission('IDS => Client : List') %}
						<li class="nav-item me-3">
							<a class="nav-link" href="{{ path('app_client_index') }}">
								<i class="bi bi-people-fill me-1"></i>
								Clients
							</a>
						</li>
					{% endif %}
					{% if app.user and app.user.hasPermission('IDS => Action : List') %}
						<li class="nav-item me-3">
							<a class="nav-link" href="{{ path('app_action_index') }}">
								<i class="bi bi-check2-square me-1"></i>
								Actions
							</a>
						</li>
					{% endif %}
					{% if app.user and app.user.hasPermission('IDS => Log : List') %}
						<li class="nav-item me-3">
							<a class="nav-link" href="{{ path('app_client_action_log_index') }}">
								<i class="bi bi-clock-history me-1"></i>
								Logs
							</a>
						</li>
					{% endif %}
					<li class="nav-item me-3">
						<a class="btn btn-sm btn-outline-secondary" href="{{ path('switch_company') }}">
							<i class="bi bi-arrow-left-circle me-1"></i>
							Changer d'entité
						</a>
					</li>
					<li class="nav-item me-3">
						<span class="nav-link disabled text-secondary">
							<i class="bi bi-person-circle me-1"></i>
							{{ app.user.email }}
						</span>
					</li>
					<li class="nav-item">
						<a class="btn btn-sm btn-outline-danger" href="{{ path('app_logout') }}">
							<i class="bi bi-box-arrow-right me-1"></i>
							Déconnexion
						</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="btn btn-sm btn-outline-primary" href="{{ path('app_login') }}">
							<i class="bi bi-box-arrow-in-right me-1"></i>
							Connexion
						</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
<div class="container">
	{% for label, messages in app.flashes %}
		{% for message in messages %}
			<div class="alert alert-{{ label }} alert-dismissible fade show shadow-sm rounded mt-2" role="alert">
				<i class="bi bi-info-circle me-1"></i>
				{{ message|raw }}
				<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Fermer"></button>
			</div>
		{% endfor %}
	{% endfor %}
</div>
 <script>
    // Disparition automatique après 5 secondes
    setTimeout(() => {
        document.querySelectorAll('.alert').forEach(alert => {
            alert.classList.remove('show');
            setTimeout(() => alert.remove(), 300);
        });
    }, 5000);
</script>
